---
const { locale, pathname, dynamicParams } = Astro.locals.__i18n;
---

<script define:vars={{ locale, pathname, dynamicParams }}>
  window.__i18n = {
    locale,
    pathname,
    dynamicParams,
  };
</script>
