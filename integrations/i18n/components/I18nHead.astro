---
import { getSwitcherData } from "i18n:astro";

const data = getSwitcherData();
---

{
  data.map(({ locale, href }) => (
    <link rel="alternate" hreflang={locale} href={Astro.url.origin + href} />
  ))
}
